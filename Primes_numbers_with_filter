
nums = range(1, 10000)
print(f"items of nums: {list(nums)}")


def is_prime(num):
    for x in range(2, num):
        if (num % x) == 0:
            return False

    return True


primes = filter(is_prime, nums)

print(f"type of Primes: {primes}")
print(f"items of Primes: {list(primes)}")

